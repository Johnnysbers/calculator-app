const numberButtons    = document.querySelectorAll('[data-number]');
const operatorButtons  = document.querySelectorAll('[data-operation]');
const equalButton      = document.querySelector('[data-equal]');
const plusMinuslButton = document.querySelector('[data-plus-minus]');
const deleteButton     = document.querySelectorAll('[data-delete]');
const deleteAllButton  = document.querySelectorAll('[data-delete-all]');
const deleteOneButton  = document.querySelectorAll('[data-delete-one]');
const prevOpText       = document.querySelectorAll('[data-prev-op]');
const currentOpText    = document.querySelectorAll('[data-current-op]');

export const operations = () => {
    
}